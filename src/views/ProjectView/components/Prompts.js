import React from 'react';
import { Box, Typography } from '@mui/material';

const Prompts = () => {
  return (
    <Box>
      <Typography variant="h6">Prompts Section</Typography>
      <Typography>This is where user prompts will be managed.</Typography>
    </Box>
  );
};

export default Prompts;
